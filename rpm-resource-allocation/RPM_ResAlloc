const { Builder, By, until, Key, Actions } = require('selenium-webdriver');
const data = require('../data/inputData');
const { login } = require('../util/login');
const assert = require('assert');
const { sideMenu } = require('../util/selector');
const { RPM_ResAlloc } = require('../util/selector');
const { writeResult } = require('../util/excelReporter');
const { clickUntil } = require('../util/clickButtonUntil');
const { takeScreenshot } = require('../util/screenshot');

async function IT_001() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(1000);

        const screenshotPath = await takeScreenshot(driver, 'test_001');
        await writeResult('RPM:ResAlloc_test_IT_001', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_001');
            await writeResult('RPM:ResAlloc_test_IT_001', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_002() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Internal Activity
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        // check elements

        const FilterLabel = await driver.findElement(By.xpath("//span[normalize-space()='Business Units']"));
        assert.strictEqual(await FilterLabel.isDisplayed(), true);
        assert.strictEqual(
            (await FilterLabel.getText()).trim(),"Business Units");
        console.log(" Business Units filter label is correct");

        FilterLabel = await driver.findElement(By.xpath("//span[normalize-space()='Allocation Type']"));
        assert.strictEqual(await FilterLabel.isDisplayed(), true);
        assert.strictEqual(
            (await FilterLabel.getText()).trim(),"Allocation Type");
        console.log(" Allocation Type filter label is correct");

        FilterLabel = await driver.findElement(By.xpath("//span[normalize-space()='Nihongo Level']"));
        assert.strictEqual(await FilterLabel.isDisplayed(), true);
        assert.strictEqual(
            (await buFilterLabel.getText()).trim(),"Nihongo Level");
        console.log(" Nihongo Level filter label is correct");

        FilterLabel = await driver.findElement(By.xpath("//span[normalize-space()='Current Assignment']"));
        assert.strictEqual(await FilterLabel.isDisplayed(), true);
        assert.strictEqual(
            (await FilterLabel.getText()).trim(),"Current Assignment");
        console.log(" Current Assignment filter label is correct");

        FilterLabel = await driver.findElement(By.xpath("//span[normalize-space()='Core Skills']"));
        assert.strictEqual(await FilterLabel.isDisplayed(), true);
        assert.strictEqual(
            (await FilterLabel.getText()).trim(),"Core Skills");
        console.log(" Core Skills filter label is correct");        

        FilterLabel = await driver.findElement(By.xpath("//span[normalize-space()='Position']"));
        assert.strictEqual(await FilterLabel.isDisplayed(), true);
        assert.strictEqual(
            (await FilterLabel.getText()).trim(),"Position");
        console.log(" Position filter label is correct");

        FilterLabel = await driver.findElement(By.xpath("//span[normalize-space()='ACTION Batch']"));
        assert.strictEqual(await FilterLabel.isDisplayed(), true);
        assert.strictEqual(
            (await FilterLabel.getText()).trim(),"ACTION Batch");
        console.log(" ACTION Batch filter label is correct");

        FilterLabel = await driver.findElement(By.xpath("//span[normalize-space()='Show/Hide Cloumns']"));
        assert.strictEqual(await FilterLabel.isDisplayed(), true);
        assert.strictEqual(
            (await FilterLabel.getText()).trim(),"Show/Hide Cloumns");
        console.log(" Show/Hide Cloumns filter label is correct");

        const clearFilterBtn = await driver.findElement(RPM_ResAlloc.BTN.clearFilterBtn);
        assert.strictEqual(await clearFilterBtn.isDisplayed(), true);
        console.log(" Clear All Filters button is present");

        const searchBox = await driver.wait(
            until.elementLocated(RPM_ResAlloc.TXTBX.searchBox),
            5000);
        assert.strictEqual(await searchBox.isDisplayed(), true);
        assert.strictEqual(await searchBox.isEnabled(), true);
        console.log(" Search box is present and ready for input");

        const exportBtn = await driver.wait(until.elementLocated(RPM_ResAlloc.BTN.exportBtn),5000);
        assert.strictEqual(await exportBtn.isDisplayed(), true);
        assert.strictEqual(await exportBtn.isEnabled(), true);
        console.log(" Export Excel button is present and clickable");

        const burgerIcon = await driver.wait(
        until.elementLocated(sideMenu.burgerIcon),5000);
        const isDisplayed = await burgerIcon.isDisplayed();
        console.log("Burger button present:", isDisplayed);

        const screenshotPath = await takeScreenshot(driver, 'test_002');
        await writeResult('RPM:ResAlloc_test_IT_002', 'PASS', screenshotPath);
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_002');
            await writeResult('RPM:ResAlloc_test_IT_002', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_004() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const search = await driver.findElement(RPM_ResAlloc.TXTBX.searchBox);
        await search.clear();
        await search.sendKeys(data.searchSampData.searchKeyword1);

        //assert
        await driver.wait(until.elementsLocated(RPM_ResAlloc.TBL.tableColumns), 10000);
        let rows = await driver.findElements(RPM_ResAlloc.tableColumns);
        let matchFound = false;

        for (let row of rows) {
            let nameCell = await row.findElement(RPM_ResAlloc.TBL.tableName);
            let cellText = await nameCell.getText();

            if (cellText.includes(keyword)) {
                matchFound = true;
                break;
            }
        }
        assert.ok(matchFound, `No table row contains the keyword: ${keyword}`);
        console.log("Keyword found in table!");

        const screenshotPath = await takeScreenshot(driver, 'test_004');
        await writeResult('RPM:ResAlloc_test_IT_004', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_004');
            await writeResult('RPM:ResAlloc_test_IT_004', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_005() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const buFilter = await driver.findElement(RPM_ResAlloc.DRPDWN.buUnitsDropDown);
        await buFilter.click();
        const clearFilter = await driver.findElement(RPM_ResAlloc.BTN.BUclearFilter);
        await clearFilter.click();
        const selectOption = await driver.findElement(By.xpath(`/html/body/div/div/div/div[2]/div/div/div[2]/div/div[1]/div/div/div[2]/div[1]`));
        await selectOption.click();
        await driver.sleep(1000);

        const allocType = await driver.findElement(RPM_ResAlloc.DRPDWN.allocTypeDropDown);
        await allocType.click();
        const clearFilter2 = await driver.findElement(RPM_ResAlloc.BTN.ATclearFilter);
        await clearFilter2.click();
        const selectOption2 = await driver.findElement(By.xpath(`/html/body/div/div/div/div[2]/div/div/div[2]/div/div[2]/div/div/div[2]/div[1]`));
        await selectOption2.click();
        await driver.sleep(1000);

        const nihongoLvl = await driver.findElement(RPM_ResAlloc.DRPDWN.nihongoLevelDropDown);
        await nihongoLvl.click();
        const clearFilter3 = await driver.findElement(RPM_ResAlloc.BTN.NLclearFilter);
        await clearFilter3.click();
        const selectOption3 = await driver.findElement(By.xpath(`/html/body/div/div/div/div[2]/div/div/div[2]/div/div[3]/div/div/div[2]/div[1]`));
        await selectOption3.click();
        await driver.sleep(1000);

        const currAssignment = await driver.findElement(RPM_ResAlloc.DRPDWN.currentAssignmentDropDown);
        await currAssignment.click();
        const clearFilter4 = await driver.findElement(RPM_ResAlloc.BTN.CAclearFilter);
        await clearFilter4.click();
        const selectOption4 = await driver.findElement(By.xpath(`/html/body/div/div/div/div[2]/div/div/div[2]/div/div[4]/div/div/div[2]/div[1]`));
        await selectOption4.click();
        await driver.sleep(1000);

        const coreSkill = await driver.findElement(RPM_ResAlloc.DRPDWN.coreSkillsDropDown);
        await coreSkill.click();
        const clearFilter5 = await driver.findElement(RPM_ResAlloc.BTN.CSclearFilter);
        await clearFilter5.click();
        const selectOption5 = await driver.findElement(By.xpath(`/html/body/div/div/div/div[2]/div/div/div[2]/div/div[5]/div/div/div[2]/div[1]`));
        await selectOption5.click();
        await driver.sleep(1000);

        const position = await driver.findElement(RPM_ResAlloc.DRPDWN.positionDropDown);
        await position.click();
        const clearFilter6 = await driver.findElement(RPM_ResAlloc.BTN.POSclearFilter);
        await clearFilter6.click();
        const selectOption6 = await driver.findElement(By.xpath(`/html/body/div/div/div/div[2]/div/div/div[2]/div/div[6]/div/div/div[2]/div[1]`));
        await selectOption6.click();
        await driver.sleep(1000);

        const action = await driver.findElement(RPM_ResAlloc.DRPDWN.actionBatchDropDown);
        await action.click();
        const clearFilter7 = await driver.findElement(RPM_ResAlloc.BTN.ABclearFilter);
        await clearFilter7.click();
        const selectOption7 = await driver.findElement(By.xpath(`/html/body/div/div/div/div[2]/div/div/div[2]/div/div[7]/div/div/div[2]/div[2]`));
        await selectOption7.click();
        await driver.sleep(1000);

        const screenshotPath = await takeScreenshot(driver, 'test_005');
        await writeResult('RPM:ResAlloc_test_IT_005', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_005');
            await writeResult('RPM:ResAlloc_test_IT_005', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_006() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        await driver.findElement(RPM_ResAlloc.DRPDWN.rowCount).click();
        await driver.findElement(By.xpath(`/html/body/div[2]/div[3]/ul/li[4]`)).click(); //click 100

        await clickUntil(driver,RPM_ResAlloc.BTN.pageFwd);
        await driver.sleep(500);
        await clickUntil(driver,RPM_ResAlloc.BTN.pageBck);
        await driver.sleep(500);

        const screenshotPath = await takeScreenshot(driver, 'test_006');
        await writeResult('RPM:ResAlloc_test_IT_006', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_006');
            await writeResult('RPM:ResAlloc_test_IT_006', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_007() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const showHide = await driver.findElement(RPM_ResAlloc.DRPDWN.showHide);
        await showHide.click();

        const clr = await driver.findElement(RPM_ResAlloc.BTN.SHclearFilter);
        await clr.click();

        for (let i = 1; i <= 22; i++) {
        const path = `/html/body/div/div/div/div[2]/div/div/div[2]/div/div[8]/div/div/div[2]/div[${i}]/input`;

        const selectSH = await driver.findElement(By.xpath(path));
        await selectSH.click();
        await driver.sleep(500);
}

        const screenshotPath = await takeScreenshot(driver, 'test_007');
        await writeResult('RPM:ResAlloc_test_IT_007', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_007');
            await writeResult('RPM:ResAlloc_test_IT_007', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_008() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        for (let i = 3; i <= 25; i++) {
            let sort = `/html/body/div/div/div/div[2]/div/div/div[4]/div/div/div[1]/div[2]/div[1]/div/div/div[${i}]`;
            let selectSort = await driver.wait(until.elementLocated(By.xpath(sort)),10000);

           await selectSort.click();  // Click the sort button
           await driver.sleep(500);  // Wait for UI to update
           await selectSort.click();  // Click the sort button again
           await driver.sleep(500);  // Wait for UI to update again
    }

        const screenshotPath = await takeScreenshot(driver, 'test_008');
        await writeResult('RPM:ResAlloc_test_IT_008', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_008');
            await writeResult('RPM:ResAlloc_test_IT_008', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_009() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

       for (let i = 3; i <= 25; i++) {
        const resize = `/html/body/div/div/div/div[2]/div/div/div[4]/div/div/div[1]/div[2]/div[1]/div/div/div[${i}]`;
        const selectResize = await driver.findElement(By.xpath(resize));

        const columnResizeHandle = await selectResize.findElement(By.css('.MuiDataGrid-columnSeparator'));

        const randomWidth = Math.floor(Math.random() * (300 - 2 + 1)) + 50;

        await driver.actions({ bridge: true })
            .dragAndDrop(columnResizeHandle, { x: randomWidth, y: 0 })
            .perform();

        console.log(`Column ${i} resized by ${randomWidth} pixels`);
        await driver.sleep(500);
    }

        const screenshotPath = await takeScreenshot(driver, 'test_009');
        await writeResult('RPM:ResAlloc_test_IT_009', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_009');
            await writeResult('RPM:ResAlloc_test_IT_009', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_010() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const resizeTimes = 10;

        for (let t = 0; t < resizeTimes; t++) {

            const randomWidth = Math.floor(Math.random() * (1920 - 300 + 1)) + 300;   
            const randomHeight = Math.floor(Math.random() * (1080 - 300 + 1)) + 300;  

            await driver.manage().window().setRect({ width: randomWidth, height: randomHeight });
            console.log(`Resized window to ${randomWidth}x${randomHeight}`);
            await driver.sleep(3000);
        }
        // Finally, maximize the window again
        await driver.manage().window().maximize();

        const screenshotPath = await takeScreenshot(driver, 'test_010');
        await writeResult('RPM:ResAlloc_test_IT_010', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_010');
            await writeResult('RPM:ResAlloc_test_IT_010', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_011() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const exportFile = await driver.findElement(RPM_ResAlloc.BTN.exportBtn);
        await exportFile.click();

        const screenshotPath = await takeScreenshot(driver, 'test_011');
        await writeResult('RPM:ResAlloc_test_IT_011', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_011');
            await writeResult('RPM:ResAlloc_test_IT_011', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_013() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);
    
        const BUDropdown = await driver.findElement(RPM_ResAlloc.DRPDWN.buUnitsDropDown);
        await BUDropdown.click();

        const BUOptions = await driver.findElements(RPM_ResAlloc.DRPDWN.buOptions);

        const expectedBUOptions = data.dropdown.buOptions;
        const actualBUOptions = [];
        for (const option of BUOptions) {
            await driver.executeScript("arguments[0].scrollIntoView(true);", option);

            let text = (await option.getText()).trim();

            if (text.endsWith('+')) {
                text = text.slice(0, -1).trim();
            }

            const items = text.split('\n').map(t => t.trim()).filter(t => t !== 'N/A' && t.length > 0);
            actualBUOptions.push(...items); // push each item separately
        }

        assert.deepStrictEqual(actualBUOptions, expectedBUOptions);
        console.log('Dropdown options match expected values!');

        const screenshotPath = await takeScreenshot(driver, 'test_013');
        await writeResult('RPM:ResAlloc_test_IT_013', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_013');
            await writeResult('RPM:ResAlloc_test_IT_013', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_014() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const ATDropDown = await driver.findElement(RPM_ResAlloc.DRPDWN.allocTypeDropDown);
        await ATDropDown.click();

        const ATOptions = await driver.findElements(RPM_ResAlloc.DRPDWN.ATOptions);

        const expectedATOptions = data.dropdown.allocationoptions;
        const actualATOptions = [];
        for (const option of ATOptions) {
            await driver.executeScript("arguments[0].scrollIntoView(true);", option);

            let text = (await option.getText()).trim();

            if (text.endsWith('+')) {
                text = text.slice(0, -1).trim();
            }

            const items = text.split('\n').map(t => t.trim()).filter(t => t !== 'N/A' && t.length > 0);
            actualATOptions.push(...items); // push each item separately
        }

        assert.deepStrictEqual(actualATOptions, expectedATOptions);
        console.log('Dropdown options match expected values!');

        const screenshotPath = await takeScreenshot(driver, 'test_014');
        await writeResult('RPM:ResAlloc_test_IT_014', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_014');
            await writeResult('RPM:ResAlloc_test_IT_014', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_015() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const NLDropDown = await driver.findElement(RPM_ResAlloc.DRPDWN.nihongoLevelDropDown);
        await NLDropDown.click();

        const NLOptions = await driver.findElements(RPM_ResAlloc.DRPDWN.NLOptions);

        const expectedNLOptions = data.dropdown.nihongo;
        const actualNLOptions = [];
        for (const option of NLOptions) {
            await driver.executeScript("arguments[0].scrollIntoView(true);", option);

            let text = (await option.getText()).trim();

            if (text.endsWith('+')) {
                text = text.slice(0, -1).trim();
            }

            const items = text.split('\n').map(t => t.trim()).filter(t => t !== 'N/A' && t.length > 0);
            actualNLOptions.push(...items); // push each item separately
        }

        assert.deepStrictEqual(actualNLOptions, expectedNLOptions);
        console.log('Dropdown options match expected values!');

        const screenshotPath = await takeScreenshot(driver, 'test_015');
        await writeResult('RPM:ResAlloc_test_IT_015', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_015');
            await writeResult('RPM:ResAlloc_test_IT_015', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_016() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const CADropDown = await driver.findElement(RPM_ResAlloc.DRPDWN.currentAssignmentDropDown);
        await CADropDown.click();

        const CAOptions = await driver.findElements(RPM_ResAlloc.DRPDWN.CAOptions);

        const expectedCAOptions = data.dropdown.currentAssignment;
        const actualCAOptions = [];
        for (const option of CAOptions) {
            await driver.executeScript("arguments[0].scrollIntoView(true);", option);

            let text = (await option.getText()).trim();

            if (text.endsWith('+')) {
                text = text.slice(0, -1).trim();
            }

            const items = text.split('\n').map(t => t.trim()).filter(t => t !== 'N/A' && t.length > 0);
            actualCAOptions.push(...items); // push each item separately
        }

        assert.deepStrictEqual(actualCAOptions, expectedCAOptions);
        console.log('Dropdown options match expected values!');

        const screenshotPath = await takeScreenshot(driver, 'test_016');
        await writeResult('RPM:ResAlloc_test_IT_016', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_016');
            await writeResult('RPM:ResAlloc_test_IT_016', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_017() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const CSDropDown = await driver.findElement(RPM_ResAlloc.DRPDWN.coreSkillsDropDown);
        await CSDropDown.click();

        const CSOptions = await driver.findElements(RPM_ResAlloc.DRPDWN.CSOptions);

        const expectedCSOptions = data.dropdown.coreSkills;
        const actualCSOptions = [];
        for (const option of CSOptions) {
            await driver.executeScript("arguments[0].scrollIntoView(true);", option);

            let text = (await option.getText()).trim();

            if (text.endsWith('+')) {
                text = text.slice(0, -1).trim();
            }

            const items = text.split('\n').map(t => t.trim()).filter(t => t !== 'N/A' && t.length > 0);
            actualCSOptions.push(...items); // push each item separately
        }

        assert.deepStrictEqual(actualCSOptions, expectedCSOptions);
        console.log('Dropdown options match expected values!');

        const screenshotPath = await takeScreenshot(driver, 'test_017');
        await writeResult('RPM:ResAlloc_test_IT_017', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_017');
            await writeResult('RPM:ResAlloc_test_IT_017', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_018() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const POSDropDown = await driver.findElement(RPM_ResAlloc.DRPDWN.positionDropDown);
        await POSDropDown.click();

        const POSOptions = await driver.findElements(RPM_ResAlloc.DRPDWN.POSOptions);

        const expectedPOSOptions = data.dropdown.position;
        const actualPOSOptions = [];
        for (const option of POSOptions) {
            await driver.executeScript("arguments[0].scrollIntoView(true);", option);

            let text = (await option.getText()).trim();

            if (text.endsWith('+')) {
                text = text.slice(0, -1).trim();
            }

            const items = text.split('\n').map(t => t.trim()).filter(t => t !== 'N/A' && t.length > 0);
            actualPOSOptions.push(...items); // push each item separately
        }

        assert.deepStrictEqual(actualPOSOptions, expectedPOSOptions);
        console.log('Dropdown options match expected values!');

        const screenshotPath = await takeScreenshot(driver, 'test_018');
        await writeResult('RPM:ResAlloc_test_IT_018', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_018');
            await writeResult('RPM:ResAlloc_test_IT_018', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_019() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const ABDropDown = await driver.findElement(RPM_ResAlloc.DRPDWN.actionBatchDropDown);
        await ABDropDown.click();

        const ABOptions = await driver.findElements(RPM_ResAlloc.DRPDWN.ABOptions);

        const expectedABOptions = data.dropdown.ACTION;
        const actualABOptions = [];
        for (const option of ABOptions) {
            await driver.executeScript("arguments[0].scrollIntoView(true);", option);

            let text = (await option.getText()).trim();

            if (text.endsWith('+')) {
                text = text.slice(0, -1).trim();
            }

            const items = text.split('\n').map(t => t.trim()).filter(t => t !== 'N/A' && t.length > 0);
            actualABOptions.push(...items); // push each item separately
        }

        assert.deepStrictEqual(actualABOptions, expectedABOptions);
        console.log('Dropdown options match expected values!');

        const screenshotPath = await takeScreenshot(driver, 'test_019');
        await writeResult('RPM:ResAlloc_test_IT_019', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_019');
            await writeResult('RPM:ResAlloc_test_IT_019', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_020() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const dropdowns = [
        { drop: RPM_ResAlloc.DRPDWN.buUnitsDropDown, clearBtn: RPM_ResAlloc.BTN.BUclearFilter, selectXPath: `/html/body/div/div/div/div[2]/div/div/div[2]/div/div[1]/div/div/div[2]/div[1]` },
        { drop: RPM_ResAlloc.DRPDWN.allocTypeDropDown, clearBtn: RPM_ResAlloc.BTN.ATclearFilter, selectXPath: `/html/body/div/div/div/div[2]/div/div/div[2]/div/div[2]/div/div/div[2]/div[1]` },
        { drop: RPM_ResAlloc.DRPDWN.nihongoLevelDropDown, clearBtn: RPM_ResAlloc.BTN.NLclearFilter, selectXPath: `/html/body/div/div/div/div[2]/div/div/div[2]/div/div[3]/div/div/div[2]/div[1]` },
        { drop: RPM_ResAlloc.DRPDWN.currentAssignmentDropDown, clearBtn: RPM_ResAlloc.BTN.CAclearFilter, selectXPath: `/html/body/div/div/div/div[2]/div/div/div[2]/div/div[4]/div/div/div[2]/div[1]` },
        { drop: RPM_ResAlloc.DRPDWN.coreSkillsDropDown, clearBtn: RPM_ResAlloc.BTN.CSclearFilter, selectXPath: `/html/body/div/div/div/div[2]/div/div/div[2]/div/div[5]/div/div/div[2]/div[1]` },
        { drop: RPM_ResAlloc.DRPDWN.positionDropDown, clearBtn: RPM_ResAlloc.BTN.POSclearFilter, selectXPath: `/html/body/div/div/div/div[2]/div/div/div[2]/div/div[6]/div/div/div[2]/div[1]` },
        { drop: RPM_ResAlloc.DRPDWN.actionBatchDropDown, clearBtn: RPM_ResAlloc.BTN.ABclearFilter, selectXPath: `/html/body/div/div/div/div[2]/div/div/div[2]/div/div[7]/div/div/div[2]/div[1]` },
    ];

        for (const item of dropdowns) {
            const dropElement = await driver.findElement(item.drop);
            await dropElement.click();
            await driver.sleep(500);
            const clearBtn = await driver.findElement(item.clearBtn);
            await clearBtn.click();
            await driver.sleep(500);
            const firstOption = await driver.findElement(By.xpath(item.selectXPath));
            await firstOption.click();
            await driver.sleep(500);
        }

        const searchBox = await driver.findElement(RPM_ResAlloc.TXTBX.searchBox);
        await searchBox.sendKeys("xxxxSample_Onlyxxxx");
        await driver.sleep(2000);
        const clearAllBtn = await driver.findElement(RPM_ResAlloc.BTN.clearFilterBtn);
        await clearAllBtn.click();

        const screenshotPath = await takeScreenshot(driver, 'test_020');
        await writeResult('RPM:ResAlloc_test_IT_020', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_020');
            await writeResult('RPM:ResAlloc_test_IT_020', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_021() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const search = await driver.findElement(RPM_ResAlloc.TXTBX.searchBox);
        await search.clear();
        await search.sendKeys(data.searchSampData.searchKeyword1);
        
        const actionBtn = await driver.findElement(RPM_ResAlloc.BTN.actionBtn);
        await actionBtn.click();

        const atssProfile = await driver.wait(until.elementLocated(RPM_ResAlloc.BTN.ATSSProfileOpt),5000)
        await driver.wait(until.elementIsVisible(atssProfile), 5000);

        const allocHistory = await driver.wait(until.elementLocated(RPM_ResAlloc.BTN.allocationHistoryOpt),5000)
        await driver.wait(until.elementIsVisible(allocHistory), 5000);

        const employeeSet = await driver.wait(until.elementLocated(RPM_ResAlloc.BTN.employeeSettingsOpt),5000)
        await driver.wait(until.elementIsVisible(employeeSet), 5000);

        const screenshotPath = await takeScreenshot(driver, 'test_021');
        await writeResult('RPM:ResAlloc_test_IT_021', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_021');
            await writeResult('RPM:ResAlloc_test_IT_021', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_022() {
    let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver);

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // Wait for Resource Allocation page
        await driver.wait(until.urlContains('resource-allocation'), 10000);
        await driver.sleep(5000);

        const search = await driver.findElement(RPM_ResAlloc.TXTBX.searchBox);
        await search.clear();
        await search.sendKeys(data.searchSampData.searchKeyword1);

        const actionBtn = await driver.findElement(RPM_ResAlloc.BTN.actionBtn);
        await actionBtn.click();

        const atssProfile = await driver.wait(
            until.elementLocated(RPM_ResAlloc.BTN.ATSSProfileOpt),
            5000
        );

        // Store original tab BEFORE click
        const originalWindow = await driver.getWindowHandle();

        await atssProfile.click();

        // Wait for new tab
        await driver.wait(async () => {
            const handles = await driver.getAllWindowHandles();
            return handles.length > 1;
        }, 10000);

        // Switch to new tab
        const handles = await driver.getAllWindowHandles();
        for (let handle of handles) {
            if (handle !== originalWindow) {
                await driver.switchTo().window(handle);
                break;
            }
        }

        // Wait for ATSS page to load
        await driver.wait(
            until.urlContains('https://atss.awsys-i.com/'),
            10000
        );

        console.log("Switched to ATSS tab");
        await driver.sleep(1000);

        const screenshotPath = await takeScreenshot(driver, 'test_022');
        await writeResult('RPM:ResAlloc_test_IT_022', 'PASS', screenshotPath);

        // Close ATSS tab
        await driver.close();

        // Switch back to original tab
        await driver.switchTo().window(originalWindow);

        console.log("Returned to original tab");


    } catch (error) {
        console.error('test failed:', error.message);

        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_022');
            await writeResult('RPM:ResAlloc_test_IT_022','FAILED',screenshotPath,error.message);
        }

    } finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_023() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);
        
        const search = await driver.findElement(RPM_ResAlloc.TXTBX.searchBox);
        await search.clear();
        await search.sendKeys(data.searchSampData.searchKeyword1);

        const actionBtn = await driver.findElement(RPM_ResAlloc.BTN.actionBtn);
        await actionBtn.click();

        const allocHistory = await driver.wait(until.elementLocated(RPM_ResAlloc.BTN.allocationHistoryOpt),5000)
        await allocHistory.click();

        // Wait for  Employee Allocation History page to load
        await driver.wait(until.urlContains('allocation-history'),10000);

        const screenshotPath = await takeScreenshot(driver, 'test_023');
        await writeResult('RPM:ResAlloc_test_IT_023', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_023');
            await writeResult('RPM:ResAlloc_test_IT_023', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_024() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const search = await driver.findElement(RPM_ResAlloc.TXTBX.searchBox);
        await search.clear();
        await search.sendKeys(data.searchSampData.searchKeyword1);

        const actionBtn = await driver.findElement(RPM_ResAlloc.BTN.actionBtn);
        await actionBtn.click();

        const allocHistory = await driver.wait(until.elementLocated(RPM_ResAlloc.BTN.allocationHistoryOpt),5000)
        await allocHistory.click();

        // Wait for  Employee Allocation History page to load
        await driver.wait(until.urlContains('allocation-history'),10000);

        const selPADropdown = await driver.wait(
            until.elementLocated(RPM_ResAlloc.SUBMENU.AHistory.DRPDWN.projectAssign), 
            10000
        );
        await selPADropdown.click();
        await driver.sleep(1000);
        await selPADropdown.sendKeys('0.25');
        await driver.actions().sendKeys(Key.ENTER).perform();
        await driver.sleep(1000);

        await driver.sleep(500);
        const PAsaveBTN = await driver.wait(
            until.elementLocated(RPM_ResAlloc.SUBMENU.AHistory.BTN.PASampleSaveBTN), 
            10000
        );
        await PAsaveBTN.click();

        const selIADropdown = await driver.wait(
            until.elementLocated(RPM_ResAlloc.SUBMENU.AHistory.DRPDWN.internalActivity), 
            10000
        );
        await selIADropdown.sendKeys('0.50');
        await driver.actions().sendKeys(Key.ENTER).perform();
        await driver.sleep(1000);

        await driver.sleep(500);
        const IAsaveBTN = await driver.wait(
            until.elementLocated(RPM_ResAlloc.SUBMENU.AHistory.BTN.IASampleSaveBTN), 
            10000
        );
        await IAsaveBTN.click();

        const PAvalue = parseFloat(await selPADropdown.getAttribute('value'));
        const IAvalue = parseFloat(await selIADropdown.getAttribute('value'));

        const BUcellVal = await driver.findElement(By.xpath(
            `/html/body/div/div/div/div[2]/div/div/div[6]/div/div/div/div[2]/div/table/tbody/tr[4]/td[9]`
        ));

        const total = PAvalue + IAvalue;

        await driver.wait(async () => {
            const text = await BUcellVal.getText();
            const currentValue = parseFloat(text.replace(/[^\d.]/g, ''));
            return currentValue === total;
        }, 10000);

        const value = parseFloat(
            (await BUcellVal.getText()).replace(/[^\d.]/g, '')
        );

        console.log(value);

        assert.strictEqual(value, total, `Cell value is not ${total}`);
        console.log("Cell value verified:", value);


        await driver.navigate().refresh();
        console.log("Browser refreshed");

        const screenshotPath = await takeScreenshot(driver, 'test_024');
        await writeResult('RPM:ResAlloc_test_IT_024', 'PASS', screenshotPath);

        const selPADropdown_new = await driver.wait(
            until.elementLocated(RPM_ResAlloc.SUBMENU.AHistory.DRPDWN.projectAssign), 
            10000);
        await selPADropdown_new.click();
        await selPADropdown_new.sendKeys('0');
        await driver.actions().sendKeys(Key.ENTER).perform();
        const PAsaveBTN_new = await driver.wait(
            until.elementLocated(RPM_ResAlloc.SUBMENU.AHistory.BTN.PASampleSaveBTN), 
            10000
        );
        await PAsaveBTN_new.click();
        const selIADropdown_new = await driver.wait(
            until.elementLocated(RPM_ResAlloc.SUBMENU.AHistory.DRPDWN.internalActivity), 
            10000);
        await selIADropdown_new.click();
        await selIADropdown_new.sendKeys('0');
        await driver.actions().sendKeys(Key.ENTER).perform();
        const IAsaveBTN_new = await driver.wait(
            until.elementLocated(RPM_ResAlloc.SUBMENU.AHistory.BTN.IASampleSaveBTN), 
            10000
        );
        await IAsaveBTN_new.click();
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_024');
            await writeResult('RPM:ResAlloc_test_IT_024', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_029() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const search = await driver.findElement(RPM_ResAlloc.TXTBX.searchBox);
        await search.clear();
        await search.sendKeys(data.searchSampData.searchKeyword1);

        const actionBtn = await driver.findElement(RPM_ResAlloc.BTN.actionBtn);
        await actionBtn.click();

        const allocHistory = await driver.wait(until.elementLocated(RPM_ResAlloc.BTN.allocationHistoryOpt),5000)
        await allocHistory.click();

        // Wait for  Employee Allocation History page to load
        await driver.wait(until.urlContains('allocation-history'),10000);

        const PAsearch = await driver.findElement(RPM_ResAlloc.SUBMENU.AHistory.TXTBX.PAsearch);
        await PAsearch.sendKeys('4GCLINICAL-AUTOMATION OFFSHORE');
        await driver.sleep(1000);

        const PAassign = await driver.findElement(RPM_ResAlloc.SUBMENU.AHistory.BTN.PAAssign);
        await driver.executeScript("arguments[0].scrollIntoView({block: 'center'});", PAassign);

        // Optional: hover first (helps frameworks that require focus)
        const actions = driver.actions({ bridge: true });
        await actions.move({ origin: PAassign }).perform();

        // Click using actions
        await actions.move({ origin: PAassign }).click().perform();
        await PAassign.click();
        console.log('button clicked');
        
        await driver.sleep(3000);
        const screenshotPath = await takeScreenshot(driver, 'test_029');
        await writeResult('RPM:ResAlloc_test_IT_029', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_029');
            await writeResult('RPM:ResAlloc_test_IT_029', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_031() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const search = await driver.findElement(RPM_ResAlloc.TXTBX.searchBox);
        await search.clear();
        await search.sendKeys(data.searchSampData.searchKeyword1);

        const actionBtn = await driver.findElement(RPM_ResAlloc.BTN.actionBtn);
        await actionBtn.click();

        const allocHistory = await driver.wait(until.elementLocated(RPM_ResAlloc.BTN.allocationHistoryOpt),5000)
        await allocHistory.click();

        // Wait for  Employee Allocation History page to load
        await driver.wait(until.urlContains('allocation-history'),10000);

        const selPADropdown = await driver.wait(
            until.elementLocated(By.xpath(`//*[@id="root"]/div/div/div[2]/div/div/div[4]/div/div/div/div[2]/div/table/tbody/tr[2]/td[9]/select`)), 
            10000
        );
        await selPADropdown.click();
        await driver.sleep(1000);
        await selPADropdown.sendKeys('0.25');
        await driver.actions().sendKeys(Key.ENTER).perform();
        await driver.sleep(1000);

        await driver.sleep(500);
        const PAsaveBTN = await driver.wait(
            until.elementLocated(By.xpath(`//*[@id="root"]/div/div/div[2]/div/div/div[4]/div/div/div/div[2]/table/tbody/tr[2]/td[1]/span[2]`)), 
            10000
        );
        await PAsaveBTN.click();

        const screenshotPath = await takeScreenshot(driver, 'test_031');
        await writeResult('RPM:ResAlloc_test_IT_031', 'PASS', screenshotPath);

        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_031');
            await writeResult('RPM:ResAlloc_test_IT_031', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_036() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const search = await driver.findElement(RPM_ResAlloc.TXTBX.searchBox);
        await search.clear();
        await search.sendKeys(data.searchSampData.searchKeyword1);

        const actionBtn = await driver.findElement(RPM_ResAlloc.BTN.actionBtn);
        await actionBtn.click();

        const allocHistory = await driver.wait(until.elementLocated(RPM_ResAlloc.BTN.allocationHistoryOpt),5000)
        await allocHistory.click();

        // Wait for  Employee Allocation History page to load
        await driver.wait(until.urlContains('allocation-history'),10000);

        const deletePAData = await driver.findElement(By.xpath(`//*[@id="root"]/div/div/div[2]/div/div/div[4]/div/div/div/div[2]/table/tbody/tr[2]/td[1]/span[1]`));
        await deletePAData.click();
        await driver.actions().sendKeys(Key.ENTER).perform();
        await driver.actions().sendKeys(Key.ENTER).perform();

        const screenshotPath = await takeScreenshot(driver, 'test_036');
        await writeResult('RPM:ResAlloc_test_IT_036', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_036');
            await writeResult('RPM:ResAlloc_test_IT_036', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_038() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const search = await driver.findElement(RPM_ResAlloc.TXTBX.searchBox);
        await search.clear();
        await search.sendKeys(data.searchSampData.searchKeyword1);

        const actionBtn = await driver.findElement(RPM_ResAlloc.BTN.actionBtn);
        await actionBtn.click();

        const allocHistory = await driver.wait(until.elementLocated(RPM_ResAlloc.BTN.allocationHistoryOpt),5000)
        await allocHistory.click();

        // Wait for  Employee Allocation History page to load
        await driver.wait(until.urlContains('allocation-history'),10000);

        const PAsearch = await driver.findElement(RPM_ResAlloc.SUBMENU.AHistory.TXTBX.PAsearch);
        await PAsearch.sendKeys('4GCLINICAL-CSS OFFSHORE');
        const PAassign = await driver.findElement(RPM_ResAlloc.SUBMENU.AHistory.BTN.PAAssign);
        await PAassign.click();

        const PAsearch2 = await driver.findElement(RPM_ResAlloc.SUBMENU.AHistory.TXTBX.PAsearch);
        await PAsearch2.sendKeys('LENOVO EDGE OFFSHORE');
        const PAassign2 = await driver.findElement(RPM_ResAlloc.SUBMENU.AHistory.BTN.PAAssign);
        await PAassign2.click();

        const screenshotPath = await takeScreenshot(driver, 'test_038');
        await writeResult('RPM:ResAlloc_test_IT_038', 'PASS', screenshotPath);

        const deleteData = await driver.findElement(By.xpath(RPM_ResAlloc.SUBMENU.AHistory.BTN.PADeleteData));
        await deleteData.click();
        await driver.actions().sendKeys(Key.ENTER).perform();

        const deleteData2 = await driver.findElement(By.xpath(RPM_ResAlloc.SUBMENU.AHistory.BTN.PADeleteData2));
        await deleteData2.click();
        await driver.actions().sendKeys(Key.ENTER).perform();
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_038');
            await writeResult('RPM:ResAlloc_test_IT_038', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_039() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const search = await driver.findElement(RPM_ResAlloc.TXTBX.searchBox);
        await search.clear();
        await search.sendKeys(data.searchSampData.searchKeyword1);

        const actionBtn = await driver.findElement(RPM_ResAlloc.BTN.actionBtn);
        await actionBtn.click();

        const allocHistory = await driver.wait(until.elementLocated(RPM_ResAlloc.BTN.allocationHistoryOpt),5000)
        await allocHistory.click();

        // Wait for  Employee Allocation History page to load
        await driver.wait(until.urlContains('allocation-history'),10000);

        const PAsearch = await driver.findElement(RPM_ResAlloc.SUBMENU.AHistory.TXTBX.PAsearch);
        await PAsearch.sendKeys('4GCLINICAL-CSS OFFSHORE');
        const PAassign = await driver.findElement(RPM_ResAlloc.SUBMENU.AHistory.BTN.PAAssign);
        await PAassign.click();

        const PAsearch2 = await driver.findElement(RPM_ResAlloc.SUBMENU.AHistory.TXTBX.PAsearch);
        await PAsearch2.sendKeys('4GCLINICAL-CSS OFFSHORE');
        const PAassign2 = await driver.findElement(RPM_ResAlloc.SUBMENU.AHistory.BTN.PAAssign);
        await PAassign2.click();

        const screenshotPath = await takeScreenshot(driver, 'test_039');
        await writeResult('RPM:ResAlloc_test_IT_039', 'PASS', screenshotPath);

        await driver.actions().sendKeys(Key.ENTER).perform();
        const deleteData = await driver.findElement(By.xpath(RPM_ResAlloc.SUBMENU.AHistory.BTN.PADeleteData));
        await deleteData.click();
        await driver.actions().sendKeys(Key.ENTER).perform();
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_039');
            await writeResult('RPM:ResAlloc_test_IT_039', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_040() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const search = await driver.findElement(RPM_ResAlloc.TXTBX.searchBox);
        await search.clear();
        await search.sendKeys(data.searchSampData.searchKeyword1);

        const actionBtn = await driver.findElement(RPM_ResAlloc.BTN.actionBtn);
        await actionBtn.click();

        const allocHistory = await driver.wait(until.elementLocated(RPM_ResAlloc.BTN.allocationHistoryOpt),5000)
        await allocHistory.click();

        // Wait for  Employee Allocation History page to load
        await driver.wait(until.urlContains('allocation-history'),10000);

        const PAsearch = await driver.findElement(RPM_ResAlloc.SUBMENU.AHistory.TXTBX.PAsearch);
        await PAsearch.sendKeys('4GCLINICAL-CSS OFFSHORE');
        const PAassign = await driver.findElement(RPM_ResAlloc.SUBMENU.AHistory.BTN.PAAssign);
        await PAassign.click();

        const selectRole = await driver.findElement(RPM_ResAlloc.SUBMENU.AHistory.DRPDWN.selectRole);
        await selectRole.sendKeys('Sub leader');
        await driver.actions().sendKeys(Key.ENTER).perform();

        const PAsearch2 = await driver.findElement(RPM_ResAlloc.SUBMENU.AHistory.TXTBX.PAsearch);
        await PAsearch2.sendKeys('4GCLINICAL-CSS OFFSHORE');
        const PAassign2 = await driver.findElement(RPM_ResAlloc.SUBMENU.AHistory.BTN.PAAssign);
        await PAassign2.click();

        const screenshotPath = await takeScreenshot(driver, 'test_040');
        await writeResult('RPM:ResAlloc_test_IT_040', 'PASS', screenshotPath);

        await driver.actions().sendKeys(Key.ENTER).perform();

        const deleteData = await driver.findElement(By.xpath(RPM_ResAlloc.SUBMENU.AHistory.BTN.PADeleteData));
        await deleteData.click();
        await driver.actions().sendKeys(Key.ENTER).perform();
        
        const deleteData2 = await driver.findElement(By.xpath(RPM_ResAlloc.SUBMENU.AHistory.BTN.PADeleteData2));
        await deleteData2.click();
        await driver.actions().sendKeys(Key.ENTER).perform();
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_040');
            await writeResult('RPM:ResAlloc_test_IT_040', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_041() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const search = await driver.findElement(RPM_ResAlloc.TXTBX.searchBox);
        await search.clear();
        await search.sendKeys(data.searchSampData.searchKeyword1);

        const actionBtn = await driver.findElement(RPM_ResAlloc.BTN.actionBtn);
        await actionBtn.click();

        const allocHistory = await driver.wait(until.elementLocated(RPM_ResAlloc.BTN.allocationHistoryOpt),5000)
        await allocHistory.click();

        // Wait for  Employee Allocation History page to load
        await driver.wait(until.urlContains('allocation-history'),10000);

        const PAsearch = await driver.findElement(RPM_ResAlloc.SUBMENU.AHistory.TXTBX.PAsearch);
        await PAsearch.sendKeys('4GCLINICAL-CSS OFFSHORE');
        const PAassign = await driver.findElement(RPM_ResAlloc.SUBMENU.AHistory.BTN.PAAssign);
        await PAassign.click();

        const selectType = await driver.findElement(RPM_ResAlloc.SUBMENU.AHistory.DRPDWN.selectType);
        await selectType.sendKeys('Project');
        await driver.actions().sendKeys(Key.ENTER).perform();

        const PAsearch2 = await driver.findElement(RPM_ResAlloc.SUBMENU.AHistory.TXTBX.PAsearch);
        await PAsearch2.sendKeys('4GCLINICAL-CSS OFFSHORE');
        const PAassign2 = await driver.findElement(RPM_ResAlloc.SUBMENU.AHistory.BTN.PAAssign);
        await PAassign2.click();

        const screenshotPath = await takeScreenshot(driver, 'test_040');
        await writeResult('RPM:ResAlloc_test_IT_040', 'PASS', screenshotPath);

        await driver.actions().sendKeys(Key.ENTER).perform();

        const deleteData = await driver.findElement(By.xpath(RPM_ResAlloc.SUBMENU.AHistory.BTN.PADeleteData));
        await deleteData.click();
        await driver.actions().sendKeys(Key.ENTER).perform();

        const deleteData2 = await driver.findElement(By.xpath(RPM_ResAlloc.SUBMENU.AHistory.BTN.PADeleteData2));
        await deleteData2.click();
        await driver.actions().sendKeys(Key.ENTER).perform();
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_040');
            await writeResult('RPM:ResAlloc_test_IT_040', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_054() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const search = await driver.findElement(RPM_ResAlloc.TXTBX.searchBox);
        await search.clear();
        await search.sendKeys(data.searchSampData.searchKeyword1);

        const actionBtn = await driver.findElement(RPM_ResAlloc.BTN.actionBtn);
        await actionBtn.click();

        const empSet = await driver.wait(until.elementLocated(RPM_ResAlloc.BTN.employeeSettingsOpt),5000)
        await empSet.click();

        // Wait for  Employee Settings page to load
        await driver.wait(until.urlContains('employee-settings'),10000);

        const screenshotPath = await takeScreenshot(driver, 'test_053');
        await writeResult('RPM:ResAlloc_test_IT_053', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_053');
            await writeResult('RPM:ResAlloc_test_IT_053', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_055() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const search = await driver.findElement(RPM_ResAlloc.TXTBX.searchBox);
        await search.clear();
        await search.sendKeys(data.searchSampData.searchKeyword1);

        const actionBtn = await driver.findElement(RPM_ResAlloc.BTN.actionBtn);
        await actionBtn.click();

        const empSet = await driver.wait(until.elementLocated(RPM_ResAlloc.BTN.employeeSettingsOpt),5000)
        await empSet.click();

        // Wait for  Employee Settings page to load
        await driver.wait(until.urlContains('employee-settings'),10000);

        const longLeaveTab = await driver.wait(until.elementLocated(RPM_ResAlloc.SUBMENU.ESettings.TAB.longLeave),10000);
        await longLeaveTab.click();

        const lastEmpDate = await driver.wait(until.elementLocated(RPM_ResAlloc.SUBMENU.ESettings.TAB.lastEmpDate),10000);
        await lastEmpDate.click();

        const screenshotPath = await takeScreenshot(driver, 'test_055');
        await writeResult('RPM:ResAlloc_test_IT_055', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_055');
            await writeResult('RPM:ResAlloc_test_IT_055', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_056() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const search = await driver.findElement(RPM_ResAlloc.TXTBX.searchBox);
        await search.clear();
        await search.sendKeys(data.searchSampData.searchKeyword1);

        const actionBtn = await driver.findElement(RPM_ResAlloc.BTN.actionBtn);
        await actionBtn.click();

        const empSet = await driver.wait(until.elementLocated(RPM_ResAlloc.BTN.employeeSettingsOpt),5000)
        await empSet.click();

        // Wait for  Employee Settings page to load
        await driver.wait(until.urlContains('employee-settings'),10000);

        const longLeaveTab = await driver.wait(until.elementLocated(RPM_ResAlloc.SUBMENU.ESettings.TAB.longLeave),10000);
        await longLeaveTab.click();

        const leaveType = await driver.findElement(RPM_ResAlloc.SUBMENU.ESettings.TAB1.DRPDWN.leaveType);
        await leaveType.click(); 
        const selectOpt = await driver.findElement(RPM_ResAlloc.SUBMENU.ESettings.TAB1.DRPDWN.selectIndef);
        await selectOpt.click();
        const typeValue = await selectOpt.getText();
        
        const date = await driver.findElement(RPM_ResAlloc.SUBMENU.ESettings.TAB1.TXTBX.date)
        await date.sendKeys(data.employeeSetting.date);

        const remarks = await driver.findElement(RPM_ResAlloc.SUBMENU.ESettings.TAB1.TXTBX.remarks)
        await remarks.sendKeys(data.employeeSetting.remarks);

        const addBtn = await driver.findElement(RPM_ResAlloc.SUBMENU.ESettings.TAB1.BTN.adLeaveBTN);
        await addBtn.click();

        await driver.sleep(5000);

        //assert type
        const typeCell = await driver.findElement(By.xpath(`/html/body/div/div/div/div[2]/div/div/div[3]/div/div[1]/div/div[4]/div/div[1]/div[2]/div[2]/div/div/div[3]`));
        const actualTypeValue = (await typeCell.getText()).trim();
        const expectedTypeValue = typeValue;
        assert.strictEqual(actualTypeValue, expectedTypeValue, 'Date value mismatch');
 
        //assert date
        const dateCell1 = await driver.findElement(By.xpath('/html/body/div/div/div/div[2]/div/div/div[3]/div/div[1]/div/div[4]/div/div[1]/div[2]/div[2]/div/div/div[4]'));
        const dateCell2 = await driver.findElement(By.xpath('/html/body/div/div/div/div[2]/div/div/div[3]/div/div[1]/div/div[4]/div/div[1]/div[2]/div[2]/div/div/div[5]'));
        const value1 = (await dateCell1.getText()).trim();
        const value2 = (await dateCell2.getText()).trim();
             // combine
        const actualDateValue = `${value1} ${value2}`;
        const expectedDateValue = data.employeeSetting.date;
        assert.strictEqual(actualDateValue, expectedDateValue, 'Date value mismatch');

        //assert remarks
        const RemarkCell = await driver.findElement(By.xpath(`/html/body/div/div/div/div[2]/div/div/div[3]/div/div[1]/div/div[4]/div/div[1]/div[2]/div[2]/div/div/div[3]`));
        const actualRemarkValue = (await RemarkCell.getText()).trim();
        const expectedRemarkValue = data.employeeSetting.date;
        assert.strictEqual(actualRemarkValue, expectedRemarkValue, 'Remarks value mismatch');

        const screenshotPath = await takeScreenshot(driver, 'test_056');
        await writeResult('RPM:ResAlloc_test_IT_056', 'PASS', screenshotPath);

    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_056');
            await writeResult('RPM:ResAlloc_test_IT_056', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_057() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const search = await driver.findElement(RPM_ResAlloc.TXTBX.searchBox);
        await search.clear();
        await search.sendKeys(data.searchSampData.searchKeyword1);

        const actionBtn = await driver.findElement(RPM_ResAlloc.BTN.actionBtn);
        await actionBtn.click();

        const empSet = await driver.wait(until.elementLocated(RPM_ResAlloc.BTN.employeeSettingsOpt),5000)
        await empSet.click();

        // Wait for  Employee Settings page to load
        await driver.wait(until.urlContains('employee-settings'),10000);

        const longLeaveTab = await driver.wait(until.elementLocated(RPM_ResAlloc.SUBMENU.ESettings.TAB.longLeave),10000);
        await longLeaveTab.click();

        const editLeave = await driver.findElement(RPM_ResAlloc.SUBMENU.ESettings.TAB1.BTN.editLeave);
        await editLeave.click();

        const leaveType = await driver.findElement(RPM_ResAlloc.SUBMENU.ESettings.TAB1.DRPDWN.leaveType);
        await leaveType.click(); 
        const selectOpt = await driver.findElement(RPM_ResAlloc.SUBMENU.ESettings.TAB1.DRPDWN.selectPat);
        await selectOpt.click();
        const typeValue = await selectOpt.getText();

        const updtBtn = await driver.findElement(RPM_ResAlloc.SUBMENU.ESettings.TAB1.BTN.updtLeaveBTN);
        await updtBtn.click();

        await driver.sleep(5000);

        //assert type
        const typeCell = await driver.findElement(By.xpath(`/html/body/div/div/div/div[2]/div/div/div[3]/div/div[1]/div/div[4]/div/div[1]/div[2]/div[2]/div/div/div[3]`));
        const actualTypeValue = (await typeCell.getText()).trim();
        const expectedTypeValue = typeValue;
        assert.strictEqual(actualTypeValue, expectedTypeValue, 'Date value mismatch');

        //assert remarks
        const RemarkCell = await driver.findElement(By.xpath(`/html/body/div/div/div/div[2]/div/div/div[3]/div/div[1]/div/div[4]/div/div[1]/div[2]/div[2]/div/div/div[3]`));
        const actualRemarkValue = (await RemarkCell.getText()).trim();
        const expectedRemarkValue = data.employeeSetting.date;
        assert.strictEqual(actualRemarkValue, expectedRemarkValue, 'Remarks value mismatch');

        const screenshotPath = await takeScreenshot(driver, 'test_057');
        await writeResult('RPM:ResAlloc_test_IT_057', 'PASS', screenshotPath);

    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_057');
            await writeResult('RPM:ResAlloc_test_IT_057', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_058() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const search = await driver.findElement(RPM_ResAlloc.TXTBX.searchBox);
        await search.clear();
        await search.sendKeys(data.searchSampData.searchKeyword1);

        const actionBtn = await driver.findElement(RPM_ResAlloc.BTN.actionBtn);
        await actionBtn.click();

        const empSet = await driver.wait(until.elementLocated(RPM_ResAlloc.BTN.employeeSettingsOpt),5000)
        await empSet.click();

        // Wait for  Employee Settings page to load
        await driver.wait(until.urlContains('employee-settings'),10000);

        const longLeaveTab = await driver.wait(until.elementLocated(RPM_ResAlloc.SUBMENU.ESettings.TAB.longLeave),10000);
        await longLeaveTab.click();

        const deleteLeave = await driver.findElement(RPM_ResAlloc.SUBMENU.ESettings.TAB1.BTN.delLeave);
        await deleteLeave.click();
        await driver.actions().sendKeys(Key.ENTER).perform();

        await driver.sleep(5000);
 
        const screenshotPath = await takeScreenshot(driver, 'test_058');
        await writeResult('RPM:ResAlloc_test_IT_058', 'PASS', screenshotPath);

    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_058');
            await writeResult('RPM:ResAlloc_test_IT_058', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_062() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const search = await driver.findElement(RPM_ResAlloc.TXTBX.searchBox);
        await search.clear();
        await search.sendKeys(data.searchSampData.searchKeyword1);

        const actionBtn = await driver.findElement(RPM_ResAlloc.BTN.actionBtn);
        await actionBtn.click();

        const empSet = await driver.wait(until.elementLocated(RPM_ResAlloc.BTN.employeeSettingsOpt),5000)
        await empSet.click();

        // Wait for  Employee Settings page to load
        await driver.wait(until.urlContains('employee-settings'),10000);

        const longLeaveTab = await driver.wait(until.elementLocated(RPM_ResAlloc.SUBMENU.AHistory.TAB.longLeave),10000);
        await longLeaveTab.click();

        const leaveType = await driver.findElement(RPM_ResAlloc.SUBMENU.ESettings.TAB1.DRPDWN.leaveType);
        await leaveType.click(); 
        const selectOpt = await driver.findElement(RPM_ResAlloc.SUBMENU.ESettings.TAB1.DRPDWN.selectIndef);
        await selectOpt.click();
        
        const date = await driver.findElement(RPM_ResAlloc.SUBMENU.ESettings.TAB1.TXTBX.date)
        await date.sendKeys(data.employeeSetting.date);

        const remarks = await driver.findElement(RPM_ResAlloc.SUBMENU.ESettings.TAB1.TXTBX.remarks)
        await remarks.sendKeys(data.employeeSetting.remarks);

        const addBtn = await driver.findElement(RPM_ResAlloc.SUBMENU.ESettings.TAB1.BTN.adLeaveBTN);
        await addBtn.click();

        //duplicate

        await leaveType.click(); 
        await selectOpt.click();
        await date.sendKeys(data.employeeSetting.date);
        await remarks.sendKeys(data.employeeSetting.remarks);
        await addBtn.click();

        await driver.sleep(5000);

        const screenshotPath = await takeScreenshot(driver, 'test_062');
        await writeResult('RPM:ResAlloc_test_IT_062', 'PASS', screenshotPath);

        await driver.actions().sendKeys(Key.ENTER).perform();
        const delLeave = await driver.findElement(RPM_ResAlloc.SUBMENU.ESettings.TAB1.BTN.delLeave);
        await delLeave.click();
        await driver.actions().sendKeys(Key.ENTER).perform();


    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_062');
            await writeResult('RPM:ResAlloc_test_IT_062', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_063() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const search = await driver.findElement(RPM_ResAlloc.TXTBX.searchBox);
        await search.clear();
        await search.sendKeys(data.searchSampData.searchKeyword1);

        const actionBtn = await driver.findElement(RPM_ResAlloc.BTN.actionBtn);
        await actionBtn.click();

        const empSet = await driver.wait(until.elementLocated(RPM_ResAlloc.BTN.employeeSettingsOpt),5000)
        await empSet.click();

        // Wait for  Employee Settings page to load
        await driver.wait(until.urlContains('employee-settings'),10000);

        const lastEmpDate = await driver.wait(until.elementLocated(RPM_ResAlloc.SUBMENU.AHistory.TAB.lastEmpDate),10000);
        await lastEmpDate.click();
        
        const date = await driver.findElement(RPM_ResAlloc.SUBMENU.ESettings.TAB2.TXTBX.date)
        await date.sendKeys(data.employeeSetting.lastDate);

        const remarks = await driver.findElement(RPM_ResAlloc.SUBMENU.ESettings.TAB2.TXTBX.remarks)
        await remarks.sendKeys(data.employeeSetting.remarks);

        const addBtn = await driver.findElement(RPM_ResAlloc.SUBMENU.ESettings.TAB2.BTN.addBTN);
        await addBtn.click();


        //assert type
        const dateCell = await driver.findElement(By.xpath(`/html/body/div/div/div/div[2]/div/div/div[3]/div/div[2]/div/div[4]/div/div[1]/div[2]/div[2]/div/div/div[3]`));
        const actualDateValue = (await dateCell.getText()).trim();
        const expectedDateValue = typeValue;
        assert.strictEqual(actualDateValue, expectedDateValue, 'Date value mismatch');

        //assert remarks
        const RemarkCell = await driver.findElement(By.xpath(`/html/body/div/div/div/div[2]/div/div/div[3]/div/div[2]/div/div[4]/div/div[1]/div[2]/div[2]/div/div/div[5]`));
        const actualRemarkValue = (await RemarkCell.getText()).trim();
        const expectedRemarkValue = data.employeeSetting.date;
        assert.strictEqual(actualRemarkValue, expectedRemarkValue, 'Remarks value mismatch');

        const screenshotPath = await takeScreenshot(driver, 'test_063');
        await writeResult('RPM:ResAlloc_test_IT_063', 'PASS', screenshotPath);

    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_063');
            await writeResult('RPM:ResAlloc_test_IT_063', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_064() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const search = await driver.findElement(RPM_ResAlloc.TXTBX.searchBox);
        await search.clear();
        await search.sendKeys(data.searchSampData.searchKeyword1);

        const actionBtn = await driver.findElement(RPM_ResAlloc.BTN.actionBtn);
        await actionBtn.click();

        const empSet = await driver.wait(until.elementLocated(RPM_ResAlloc.BTN.employeeSettingsOpt),5000)
        await empSet.click();

        // Wait for  Employee Settings page to load
        await driver.wait(until.urlContains('employee-settings'),10000);

        const lastEmpDate = await driver.wait(until.elementLocated(RPM_ResAlloc.SUBMENU.AHistory.TAB.lastEmpDate),10000);
        await lastEmpDate.click();

        const updtData = await driver.findElement(RPM_ResAlloc.SUBMENU.ESettings.TAB2.BTN.updtBTN);
        await updtData.click();
        
        const date = await driver.findElement(RPM_ResAlloc.SUBMENU.ESettings.TAB2.TXTBX.date)
        await date.sendKeys(data.employeeSetting.lastDate2);

        const remarks = await driver.findElement(RPM_ResAlloc.SUBMENU.ESettings.TAB2.TXTBX.remarks)
        await remarks.sendKeys(data.employeeSetting.remarks2);

        const addBtn = await driver.findElement(RPM_ResAlloc.SUBMENU.ESettings.TAB2.BTN.addBTN);
        await addBtn.click();

        //assert type
        const dateCell = await driver.findElement(By.xpath(`/html/body/div/div/div/div[2]/div/div/div[3]/div/div[2]/div/div[4]/div/div[1]/div[2]/div[2]/div/div/div[3]`));
        const actualDateValue = (await dateCell.getText()).trim();
        const expectedDateValue = data.employeeSetting.lastDate2;
        assert.strictEqual(actualDateValue, expectedDateValue, 'Date value mismatch');

        //assert remarks
        const RemarkCell = await driver.findElement(By.xpath(`/html/body/div/div/div/div[2]/div/div/div[3]/div/div[2]/div/div[4]/div/div[1]/div[2]/div[2]/div/div/div[5]`));
        const actualRemarkValue = (await RemarkCell.getText()).trim();
        const expectedRemarkValue = data.employeeSetting.remarks2;
        assert.strictEqual(actualRemarkValue, expectedRemarkValue, 'Remarks value mismatch');

        const screenshotPath = await takeScreenshot(driver, 'test_064');
        await writeResult('RPM:ResAlloc_test_IT_064', 'PASS', screenshotPath);

    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_064');
            await writeResult('RPM:ResAlloc_test_IT_064', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_066() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const search = await driver.findElement(RPM_ResAlloc.TXTBX.searchBox);
        await search.clear();
        await search.sendKeys(data.searchSampData.searchKeyword1);

        const actionBtn = await driver.findElement(RPM_ResAlloc.BTN.actionBtn);
        await actionBtn.click();

        const empSet = await driver.wait(until.elementLocated(RPM_ResAlloc.BTN.employeeSettingsOpt),5000)
        await empSet.click();

        // Wait for  Employee Settings page to load
        await driver.wait(until.urlContains('employee-settings'),10000);

        const lastEmpDate = await driver.wait(until.elementLocated(RPM_ResAlloc.SUBMENU.ESettings.TAB.lastEmpDate),10000);
        await lastEmpDate.click();

        const deleteEmpData = await driver.findElement(RPM_ResAlloc.SUBMENU.ESettings.TAB2.BTN.delBTN);
        await deleteEmpData.click();
        
        await driver.actions().sendKeys(Key.ENTER).perform();

        const screenshotPath = await takeScreenshot(driver, 'test_066');
        await writeResult('RPM:ResAlloc_test_IT_066', 'PASS', screenshotPath);

    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_066');
            await writeResult('RPM:ResAlloc_test_IT_066', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_068() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(5000);

        const search = await driver.findElement(RPM_ResAlloc.TXTBX.searchBox);
        await search.clear();
        await search.sendKeys(data.searchSampData.searchKeyword1);

        const actionBtn = await driver.findElement(RPM_ResAlloc.BTN.actionBtn);
        await actionBtn.click();
        
        const empSet = await driver.wait(until.elementLocated(RPM_ResAlloc.BTN.employeeSettingsOpt),5000)
        await empSet.click();

        // Wait for  Employee Settings page to load
        await driver.wait(until.urlContains('employee-settings'),10000);

        const lastEmpDate = await driver.wait(until.elementLocated(RPM_ResAlloc.SUBMENU.ESettings.TAB.lastEmpDate),10000);
        await lastEmpDate.click();
        
        const date = await driver.findElement(RPM_ResAlloc.SUBMENU.ESettings.TAB2.TXTBX.date)
        await date.sendKeys(data.employeeSetting.lastDate);

        const remarks = await driver.findElement(RPM_ResAlloc.SUBMENU.ESettings.TAB2.TXTBX.remarks)
        await remarks.sendKeys(data.employeeSetting.remarks);

        const addBtn = await driver.findElement(RPM_ResAlloc.SUBMENU.ESettings.TAB2.BTN.addBTN);
        await addBtn.click();

        //duplicate data
        await lastEmpDate.click();
        await date.sendKeys(data.employeeSetting.lastDate);
        await remarks.sendKeys(data.employeeSetting.remarks);
        await addBtn.click();

        const screenshotPath = await takeScreenshot(driver, 'test_068');
        await writeResult('RPM:ResAlloc_test_IT_068', 'PASS', screenshotPath);

        await driver.actions().sendKeys(Key.ENTER).perform();
        const delLeave = await driver.findElement(RPM_ResAlloc.SUBMENU.ESettings.TAB1.BTN.delLeave);
        await delLeave.click();
        await driver.actions().sendKeys(Key.ENTER).perform();

    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_068');
            await writeResult('RPM:ResAlloc_test_IT_068', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

module.exports = { 
     IT_001,
     IT_002,
     IT_004,
     IT_005,
     IT_006,
     IT_007,
     IT_008,
     IT_009,
     IT_010,
     IT_011,
     IT_013,
     IT_014,
     IT_015,
     IT_016,
     IT_017,
     IT_018,
     IT_019,
     IT_020,
     IT_021,
     IT_022,
     IT_023,
     IT_024,
     IT_029,
     IT_031,
     IT_036,
     IT_038,
     IT_039,
     IT_040,
     IT_041,
     IT_054,
     IT_055,
     IT_056,
     IT_057,
     IT_058,
     IT_062,
     IT_063,
     IT_064,
     IT_066,
     IT_068,
    };
