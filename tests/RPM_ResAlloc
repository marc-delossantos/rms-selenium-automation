const { Builder, By, until, Key } = require('selenium-webdriver');
const data = require('../data/inputData');
const { login } = require('../util/login');
const {assertTable} = require('../util/tableCheck');
const { sideMenu } = require('../util/selector');
const { RPM_ResAlloc } = require('../util/selector');
const { writeResult } = require('../util/excelReporter');
const { clickUntil } = require('../util/clickButtonUntil');
const { takeScreenshot } = require('../util/screenshot');

async function IT_001() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(1000);

        const screenshotPath = await takeScreenshot(driver, 'test_001');
        await writeResult('RPM:ResAlloc_test_IT_001', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_001');
            await writeResult('RPM:ResAlloc_test_IT_001', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_002() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Internal Activity
        await driver.wait(until.urlContains('internal-activities'), 10000); //wait for screen to load
        await driver.sleep(1000);

        // check elements

        const breadcrumbElements = await driver.findElements(By.xpath("//nav[@aria-label='breadcrumb']//li"));
        const actualBreadcrumbs = [];
        for (const item of breadcrumbElements) {
        actualBreadcrumbs.push((await item.getText()).trim());
        }
        assert.deepStrictEqual(actualBreadcrumbs, expectedBreadcrumbs);
        console.log(" Breadcrumbs are correct");
        
        const FilterLabel = await driver.findElement(By.xpath("//span[normalize-space()='Business Units']"));
        assert.strictEqual(await FilterLabel.isDisplayed(), true);
        assert.strictEqual(
            (await FilterLabel.getText()).trim(),"Business Units");
        console.log(" Business Units filter label is correct");

        FilterLabel = await driver.findElement(By.xpath("//span[normalize-space()='Allocation Type']"));
        assert.strictEqual(await FilterLabel.isDisplayed(), true);
        assert.strictEqual(
            (await FilterLabel.getText()).trim(),"Allocation Type");
        console.log(" Allocation Type filter label is correct");

        FilterLabel = await driver.findElement(By.xpath("//span[normalize-space()='Nihongo Level']"));
        assert.strictEqual(await FilterLabel.isDisplayed(), true);
        assert.strictEqual(
            (await buFilterLabel.getText()).trim(),"Nihongo Level");
        console.log(" Nihongo Level filter label is correct");

        FilterLabel = await driver.findElement(By.xpath("//span[normalize-space()='Current Assignment']"));
        assert.strictEqual(await FilterLabel.isDisplayed(), true);
        assert.strictEqual(
            (await FilterLabel.getText()).trim(),"Current Assignment");
        console.log(" Current Assignment filter label is correct");

        FilterLabel = await driver.findElement(By.xpath("//span[normalize-space()='Core Skills']"));
        assert.strictEqual(await FilterLabel.isDisplayed(), true);
        assert.strictEqual(
            (await FilterLabel.getText()).trim(),"Core Skills");
        console.log(" Core Skills filter label is correct");        

        FilterLabel = await driver.findElement(By.xpath("//span[normalize-space()='Position']"));
        assert.strictEqual(await FilterLabel.isDisplayed(), true);
        assert.strictEqual(
            (await FilterLabel.getText()).trim(),"Position");
        console.log(" Position filter label is correct");

        FilterLabel = await driver.findElement(By.xpath("//span[normalize-space()='ACTION Batch']"));
        assert.strictEqual(await FilterLabel.isDisplayed(), true);
        assert.strictEqual(
            (await FilterLabel.getText()).trim(),"ACTION Batch");
        console.log(" ACTION Batch filter label is correct");

        FilterLabel = await driver.findElement(By.xpath("//span[normalize-space()='Show/Hide Cloumns']"));
        assert.strictEqual(await FilterLabel.isDisplayed(), true);
        assert.strictEqual(
            (await FilterLabel.getText()).trim(),"Show/Hide Cloumns");
        console.log(" Show/Hide Cloumns filter label is correct");

        const clearFilterBtn = await driver.findElement(RPM_ResAlloc.clearFilterBtn);
        assert.strictEqual(await clearFilterBtn.isDisplayed(), true);
        console.log(" Clear All Filters button is present");

        const searchBox = await driver.wait(
            until.elementLocated(RPM_ResAlloc.searchBox),
            5000);
        assert.strictEqual(await searchBox.isDisplayed(), true);
        assert.strictEqual(await searchBox.isEnabled(), true);
        console.log(" Search box is present and ready for input");

        const exportBtn = await driver.wait(until.elementLocated(RPM_ResAlloc.exportBtn),5000);
        assert.strictEqual(await exportBtn.isDisplayed(), true);
        assert.strictEqual(await exportBtn.isEnabled(), true);
        console.log(" Export Excel button is present and clickable");

        const expectedColumns = [
        "Action",
        "Employee Number",
        "Name",
        "NickName",
        "Action Batch",
        "Gender",
        "Position",
        "BU Official",
        "BU Assigned",
        "Project Assignments",
        "Current Assignment",
        "Internal Activities",
        "Assigned Until",
        "Efficiency",
        "YoE(IND)",
        "YoE(AWS)",
        "YoE(JPN)",
        "Onsite Pref",
        "Nihongo",
        "Certifications",
        "Core Skills",
        "Status",
        "Allocation"
        ];

        const headers = await driver.findElements(RPM_ResAlloc.tableColumns);
        const headerFields = [];
        for (const h of headers) {
            const field = await h.getAttribute("data-field");
            if (field) headerFields.push(field);
        }
        console.log("Columns found:", headerFields);
        // Assert columns match
        const allColumnsPresent = expectedColumns.every(col => headerFields.includes(col));
        console.log("All expected columns present:", allColumnsPresent);

        const burgerIcon = await driver.wait(
        until.elementLocated(sideMenu.burgerIcon),5000);
        const isDisplayed = await burgerIcon.isDisplayed();
        console.log("Burger button present:", isDisplayed);

        const screenshotPath = await takeScreenshot(driver, 'test_002');
        await writeResult('RPM:ResAlloc_test_IT_002', 'PASS', screenshotPath);
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_002');
            await writeResult('RPM:ResAlloc_test_IT_002', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_004() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(1000);

        const search = await driver.findElement(RPM_ResAlloc.searchBox);
        await search.clear();
        await search.sendKeys(data.searchSampData.searchKeyword1);

        //assert
        await driver.wait(until.elementsLocated(RPM_ResAlloc.tableColumns), 10000);
        let rows = await driver.findElements(RPM_ResAlloc.tableColumns);
        let matchFound = false;

        for (let row of rows) {
            let nameCell = await row.findElement(RPM_ResAlloc.tableName);
            let cellText = await nameCell.getText();

            if (cellText.includes(keyword)) {
                matchFound = true;
                break;
            }
        }
        assert.ok(matchFound, `No table row contains the keyword: ${keyword}`);
        console.log("Keyword found in table!");

        const screenshotPath = await takeScreenshot(driver, 'test_004');
        await writeResult('RPM:ResAlloc_test_IT_004', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_004');
            await writeResult('RPM:ResAlloc_test_IT_004', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_005() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(1000);

        const buFilter = await driver.findElement(RPM_ResAlloc.DRPDWN.buUnitsDropDown);
        await buFilter.click();
        const clearFilter = await driver.findElement(RPM_ResAlloc.BTN.BUclearFilter);
        await clearFilter.click();
        const selectOption = await driver.findElement(By.xpath(`//div[contains(@class,'option') and .//span[text()='${data.dropdownValue.buOpt}']]`));
        await selectOption.click();

        const allocType = await driver.findElement(RPM_ResAlloc.DRPDWN.allocTypeDropDown);
        await allocType.click();
        clearFilter = await driver.findElement(RPM_ResAlloc.BTN.ATclearFilter);
        await clearFilter.click();
        selectOption = await driver.findElement(By.xpath(`//div[contains(@class,'option') and .//span[text()='${data.dropdownValue.allocType}']]`));
        await selectOption.click();

        const nihongoLvl = await driver.findElement(RPM_ResAlloc.nihongoLvlDropDown);
        await nihongoLvl.click();
        clearFilter = await driver.findElement(RPM_ResAlloc.BTN.NLclearFilter);
        await clearFilter.click();
        selectOption = await driver.findElement(By.xpath(`//div[contains(@class,'option') and .//span[text()='${data.dropdownValue.nihongoLvl}']]`));
        await selectOption.click();

        const currAssignment = await driver.findElement(RPM_ResAlloc.DRPDWN.currentAssignmentDropDown);
        await currAssignment.click();
        clearFilter = await driver.findElement(RPM_ResAlloc.BTN.CAclearFilter);
        await clearFilter.click();
        selectOption = await driver.findElement(By.xpath(`//div[contains(@class,'option') and .//span[text()='${data.dropdownValue.currAssignment}']]`));
        await selectOption.click();

        const coreSkill = await driver.findElement(RPM_ResAlloc.DRPDWN.coreSkillDropDown);
        await coreSkill.click();
        clearFilter = await driver.findElement(RPM_ResAlloc.BTN.CSclearFilter);
        await clearFilter.click();
        selectOption = await driver.findElement(By.xpath(`//div[contains(@class,'option') and .//span[text()='${data.dropdownValue.currAssignment}']]`));
        await selectOption.click();


        const screenshotPath = await takeScreenshot(driver, 'test_005');
        await writeResult('RPM:ResAlloc_test_IT_005', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_005');
            await writeResult('RPM:ResAlloc_test_IT_005', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_006() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(1000);

        const expectedRowCount = data.dropdown.rowCount;

        const rowCount = await driver.findElements(RPM_ResAlloc.DRPDWN.rowCount);

        const actualRowCount = [];
        for (const option of rowCount) {
            actualRowCount.push((await option.getText()).trim());
        }

        assert.deepStrictEqual(actualSdGrpOptions, expectedRowCount);
        console.log('Dropdown options match expected values!');

        await clickUntil(RPM_ResAlloc.BTN.pageFwd);
        await driver.sleep(500);
        await clickUntil(RPM_ResAlloc.BTN.pageBck);
        await driver.sleep(500);

        const screenshotPath = await takeScreenshot(driver, 'test_006');
        await writeResult('RPM:ResAlloc_test_IT_006', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_006');
            await writeResult('RPM:ResAlloc_test_IT_006', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_007() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(1000);

        const expectedShowHide = data.dropdown.showHide_RPMResourceAlloc;

        const showHide = await driver.findElements(RPM_ResAlloc.DRPDWN.showHide);

        const actualShowHide = [];
        for (const option of showHide) {
            actualShowHide.push((await option.getText()).trim());
        }

        assert.deepStrictEqual(actualShowHide, expectedShowHide);
        console.log('Dropdown options match expected values!');

        await showHide.click();
        const clr = await driver.findElement(RPM_ResAlloc.BTN.SHclearFilter);
        await clr.click();

        for (let i = 1; i <= 22; i++) {
        const path = `/html/body/div/div/div/div[2]/div/div/div[2]/div/div[8]/div/div/div[2]/div[${i}]/input`;

        const selectSH = await driver.findElement(By.xpath(path));
        await selectSH.click();
        await driver.sleep(500);
}

        const screenshotPath = await takeScreenshot(driver, 'test_007');
        await writeResult('RPM:ResAlloc_test_IT_007', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_007');
            await writeResult('RPM:ResAlloc_test_IT_007', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_008() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(1000);

        for (let i = 1; i <= 22; i++) {
        const sort = `/html/body/div/div/div/div[2]/div/div/div[4]/div/div/div[1]/div[2]/div[1]/div/div/div[${i}]/div[1]/div/div[2]/button`;
        const selectSort = await driver.findElement(By.xpath(sort));
        await selectSort.click();
        await driver.sleep(500);
        }

        const screenshotPath = await takeScreenshot(driver, 'test_008');
        await writeResult('RPM:ResAlloc_test_IT_008', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_008');
            await writeResult('RPM:ResAlloc_test_IT_008', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_009() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(1000);

        const columnResizeHandle = await driver.findElement(
            By.xpath('/html/body/div/div/div/div[2]/div/div/div[4]/div/div/div[1]/div[2]/div[1]/div/div/div[4]')
        );

        await driver.actions({ bridge: true })
            .dragAndDrop(columnResizeHandle, { x: 100, y: 0 })
            .perform();

        const screenshotPath = await takeScreenshot(driver, 'test_009');
        await writeResult('RPM:ResAlloc_test_IT_009', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_009');
            await writeResult('RPM:ResAlloc_test_IT_009', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_010() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(1000);

        const sizes = [
        { width: 1920, height: 1080 },
        { width: 1366, height: 768 },
        { width: 768, height: 1024 }
        ];

        for (const size of sizes) {
        await driver.manage().window().setRect(size);
        await driver.sleep(1000);
        }

        // Finally, maximize the window again
        await driver.manage().window().maximize();

        const screenshotPath = await takeScreenshot(driver, 'test_010');
        await writeResult('RPM:ResAlloc_test_IT_010', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_010');
            await writeResult('RPM:ResAlloc_test_IT_010', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_011() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(1000);

        const sizes = [
        { width: 1920, height: 1080 },
        { width: 1366, height: 768 },
        { width: 768, height: 1024 }
        ];

        for (const size of sizes) {
        await driver.manage().window().setRect(size);
        await driver.sleep(1000);
        }

        // Finally, maximize the window again
        await driver.manage().window().maximize();

        const screenshotPath = await takeScreenshot(driver, 'test_011');
        await writeResult('RPM:ResAlloc_test_IT_011', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_011');
            await writeResult('RPM:ResAlloc_test_IT_011', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_013() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(1000);

        const expectedBUOptions = data.dropdown.buOptions;
        const BUOptions = await driver.findElements(RPM_ResAlloc.DRPDWN.buUnitsDropDown);

        const actualBUOptions = [];
        for (const option of BUOptions) {
            actualBUOptions.push((await option.getText()).trim());
        }

        assert.deepStrictEqual(actualBUOptions, expectedBUOptions);
        console.log('Dropdown options match expected values!');

        const screenshotPath = await takeScreenshot(driver, 'test_013');
        await writeResult('RPM:ResAlloc_test_IT_013', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_013');
            await writeResult('RPM:ResAlloc_test_IT_013', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_014() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(1000);

        const expectedATOptions = data.dropdown.allocationoptions;
        const ATOptions = await driver.findElements(RPM_ResAlloc.DRPDWN.allocTypeDropDown);

        const actualATOptions = [];
        for (const option of ATOptions) {
            actualATOptions.push((await option.getText()).trim());
        }

        assert.deepStrictEqual(actualATOptions, expectedATOptions);
        console.log('Dropdown options match expected values!');

        const screenshotPath = await takeScreenshot(driver, 'test_014');
        await writeResult('RPM:ResAlloc_test_IT_014', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_014');
            await writeResult('RPM:ResAlloc_test_IT_014', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_015() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(1000);

        const expectedNLOptions = data.dropdown.nihongo;
        const NLOptions = await driver.findElements(RPM_ResAlloc.DRPDWN.nihongoLevelDropDown);

        const actualNLOptions = [];
        for (const option of NLOptions) {
            actualNLOptions.push((await option.getText()).trim());
        }

        assert.deepStrictEqual(actualNLOptions, expectedNLOptions);
        console.log('Dropdown options match expected values!');

        const screenshotPath = await takeScreenshot(driver, 'test_015');
        await writeResult('RPM:ResAlloc_test_IT_015', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_015');
            await writeResult('RPM:ResAlloc_test_IT_015', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_016() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(1000);

        const expectedCAOptions = data.dropdown.currentAssignment;
        const CAOptions = await driver.findElements(RPM_ResAlloc.DRPDWN.currentAssignmentDropDown);

        const actualCAOptions = [];
        for (const option of CAOptions) {
            actualCAOptions.push((await option.getText()).trim());
        }

        assert.deepStrictEqual(actualCAOptions, expectedCAOptions);
        console.log('Dropdown options match expected values!');

        const screenshotPath = await takeScreenshot(driver, 'test_016');
        await writeResult('RPM:ResAlloc_test_IT_016', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_016');
            await writeResult('RPM:ResAlloc_test_IT_016', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_017() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(1000);

        const expectedCAOptions = data.dropdown.currentAssignment;
        const CSOptions = await driver.findElements(RPM_ResAlloc.DRPDWN.coreSkillsDropDown);

        const actualCSOptions = [];
        for (const option of CSOptions) {
            actualCSOptions.push((await option.getText()).trim());
        }

        assert.deepStrictEqual(actualCSOptions, expectedCSOptions);
        console.log('Dropdown options match expected values!');

        const screenshotPath = await takeScreenshot(driver, 'test_017');
        await writeResult('RPM:ResAlloc_test_IT_017', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_017');
            await writeResult('RPM:ResAlloc_test_IT_017', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_018() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(1000);

        const expectedPOSOptions = data.dropdown.position;
        const POSOptions = await driver.findElements(RPM_ResAlloc.DRPDWN.positionDropDown);

        const actualPOSOptions = [];
        for (const option of POSOptions) {
            actualPOSOptions.push((await option.getText()).trim());
        }

        assert.deepStrictEqual(actualPOSOptions, expectedPOSOptions);
        console.log('Dropdown options match expected values!');

        const screenshotPath = await takeScreenshot(driver, 'test_018');
        await writeResult('RPM:ResAlloc_test_IT_018', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_018');
            await writeResult('RPM:ResAlloc_test_IT_018', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

async function IT_019() {
      let driver;
    try {
        driver = await new Builder().forBrowser('chrome').build();
        await driver.manage().window().maximize();

        await login(driver); //go login

        const sideMenuIA = await driver.findElement(sideMenu.resourceAlloc);
        await sideMenuIA.click();

        // --- Wait for Resource & Project Management/Resource Allocation
        await driver.wait(until.urlContains('resource-allocation'), 10000); //wait for screen to load
        await driver.sleep(1000);

        const expectedABOptions = data.dropdown.ACTION;
        const ABOptions = await driver.findElements(RPM_ResAlloc.DRPDWN.actionBatchDropDown);

        const actualABOptions = [];
        for (const option of ABOptions) {
            actualABOptions.push((await option.getText()).trim());
        }

        assert.deepStrictEqual(actualABOptions, expectedABOptions);
        console.log('Dropdown options match expected values!');

        const screenshotPath = await takeScreenshot(driver, 'test_019');
        await writeResult('RPM:ResAlloc_test_IT_019', 'PASS', screenshotPath);
        
    } catch (error) {
        console.error('test failed:', error.message);
        let screenshotPath;
        if (driver) {
            screenshotPath = await takeScreenshot(driver, 'test_019');
            await writeResult('RPM:ResAlloc_test_IT_019', 'FAILED', screenshotPath, error.message); // <-- Include screenshot for FAIL
        }
    }finally {
        if (driver) {
            await driver.sleep(3000);
            await driver.quit();
        }
    }
}

module.exports = { 
     IT_001,
     IT_002,
     IT_004,
     IT_005,
     IT_006,
     IT_007,
     IT_008,
     IT_009,
     IT_010,
     IT_011,
     IT_013,
     IT_014,
     IT_015,
     IT_016,
     IT_017,
     IT_018,
     IT_019,
    };
